import { View } from "react-native";

const BookingSuccess = ({}: BookingSuccessProps) => {
  return <View></View>;
};

type BookingSuccessProps = {
  url?: string;
};

export default BookingSuccess;
